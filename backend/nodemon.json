{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node --transpile-only src/server.ts",
  "delay": 500,
  "env": {
    "NODE_ENV": "development"
  }
}